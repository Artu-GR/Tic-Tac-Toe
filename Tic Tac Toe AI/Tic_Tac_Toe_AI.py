

#Create a position randomizer between 1 and 9 to determine the next move
import random
import Tic_Tac_Toe as TTT

class AI():
    def AI_Move(TTT.board):
        pos = random.randint(1, 9)
        if pos == 1 and TTT.board[0][0] == "--":
            
        if pos == 2 and TTT.board[0][1] == "--":
            #Fix me
        if pos == 3 and TTT.board[0][2] == "--":
            #Fix me
        if pos == 4 and TTT.board[1][] == "--":
            #Fix me
        if pos == 5 and TTT.board[1][1] == "--":
            #Fix me
        if pos == 6 and TTT.board[1][2] == "--":
            #Fix me
        if pos == 7 and TTT.board[3][0] == "--":
            #Fix me
        if pos == 8 and TTT.board[3][1] == "--":
            #Fix me
        if pos == 9 and TTT.board[3][2] == "--":
            #Fix me
